import { createTheme } from '@mui/material';

const darkTheme = createTheme({
  palette: {
    mode: 'dark',
    background: {
      default: '#020e25',
      paper: '#020e25',
    },
  },
});

export default darkTheme;
